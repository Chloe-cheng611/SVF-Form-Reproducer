# 実装状況

更新日: 2026-09-14

本プロダクトの目的は、帳票の画像・PDFを解析し、SVF Designerで編集可能なFormData XMLへ変換することである。現行の公開実装では「画像・PDFから候補を抽出する経路」と「対象版のベースXMLへ差分を適用して検証する経路」が分かれており、候補から新規XMLまでの一括接続は未完了である。

基本定義書・基本設計書0.3を実装目標として継続する。[SVF実装評価レビューと改善計画](doc/SVF実装評価レビューと改善計画.docx)のF01〜F12を実装し、再現ケースR01〜R20を受入条件を期待値とする回帰試験へ移した。修正順序1〜4（誤対象編集の防止、入力同一性と実行状態、保全・文字コード・単位・OS対応、通常操作への接続）を完了した。修正順序5の実機受入は未実施である。

進捗は「関数あり」「通常経路へ接続済み」「異常系確認済み」「実機受入済み」で分ける。コードが存在することを実装完了に置き換えない。**本表の「実装済み」はローカルの回帰試験までを指し、実機受入済みを意味しない。**

| TODO | 現状 | 残る実装・検証 |
|---|---|---|
| T01 profile・実行手順 | 実装済み・異常系確認済み | profileの型・enum・有限数・未知キーを読込境界で検査。ロック実装をPOSIX/Windowsで分離し遅延選択。host_osを必須化。Windows実機と対象SVF版・SP・機種・素材の登録は未実施 |
| T02 few-shot索引 | 接続済み | 索引生成、選択、LLM要求への同梱を接続。記法例を業務要件や互換性根拠にしない方針を維持。実LLMでの運用確認は未実施 |
| T03 D・N・配置比較 | 比較計画生成あり・実機未実施 | 対象Designer・SVFで実行し、期待する明細順・位置と証拠を登録する作業が残る |
| T04 項目契約・0件 | 実装済み・異常系確認済み | 欠落セル、重複・過不足列、空値、NULL、日付・十進の字句規則を実行前に検査。skip／emit_formの実機確認は未実施 |
| T05 元DOM・IR 1.2 | 実装済み・異常系確認済み | バッチ中のID固定、Shift_JIS等の厳密復号、全文字コードでのDTD・実体拒否、未知要素・属性の保全。Designer往復は未実施 |
| T06 許可差分 | 実装済み・異常系確認済み | 削除子孫の参照拒否、備考の対象範囲照合、追加ひな形の子孫検査、生成前検査。移動後も同一実体の編集を許可 |
| T07 座標・種別検査 | 実装済み・異常系確認済み | Paper/@resolutionとprofileのDPI照合、heightUnit・heightからの有効ピッチ、NaN等の拒否。対象版XMLとの座標校正は実機 |
| T08 表示行数・優先順位 | 実装済み・接続済み | 備考解決をCLI／GUIの生成経路へ接続。変更文は新値を採用し、小数・boolを拒否。DとNを別に保持 |
| T09 Record・罫線 | 接続済み・一部未対応 | layout結果のピッチを参考表示へ接続。単位未登録時は容量未確定。罫線周期の描画反映と複数Recordの容量算定は未対応 |
| T10 構造検査・Issue | 実装済み・異常系確認済み | ルート、対応親子、有限座標、リンク循環、曖昧名、項目契約を検査。errorで確定・出力を停止 |
| T11 PDF・画像抽出 | 実装済み・実測未検証 | bboxの幅・高さ、PDF行列合成・CropBox原点・回転・UserUnit、画像mm変換、構造化OCR、スキャンPDF経路、timeoutと取消。pypdf／Pillow未導入のため実PDF・実画像での測定は未実施 |
| T12 固定・明細候補 | 部分実装 | 列の整列・行間隔による候補化と未確定の保持を実装。罫線の抽出と罫線を含む候補化は未対応 |
| T13 D参考SVG | 実装済み・一部未対応 | Recordの子要素・Fieldを相対配置で反復。N=0でデータ行を描かない。打切りは実描画件数の確定後に通知。黒帯白文字など対象仕様固有の表現は未対応 |
| T14 備考版・取消 | 実装済み・接続済み | 対象選択、自然文解決、修正・取消、曖昧時のIssue化をCLI／GUIへ接続 |
| T15 LLM Proposal | XML修正経路へ接続済み | request_proposalとCommandProposalProviderをCLI／GUIへ接続。対象範囲、応答の版一致、生XML断片の参考扱いを検査。画像・PDFの候補から新規XMLを作る入力契約と実LLM運用は未実施 |
| T16 GUI・CLI | XML修正経路は統合試験あり | ベースXML→対象選択→備考→提案→検査→生成→参考表示を共通serviceへ統合。画像・PDFの抽出と候補生成はCLIにあるが、その候補を新規XML生成へ渡すGUI／CLI経路は未接続。GUIのウィジェット層はヘッドレスで実行していない |
| T17 件数・設定回帰 | 実装済み・実機未実施 | 異なる領域高と行高の2設定でN=0、1、2、C−1、C、C＋1、2C＋1とDの2値を確認。実出力の期待順と位置の照合は実機 |
| T18 Job・CSV | 実装済み・異常系確認済み | CSV内容ハッシュ、実行用入力の固定、form_revision照合、CSVの過不足・重複列・値の字句規則 |
| T19 RuntimeResult・受入記録 | 実装済み・異常系確認済み | timeout出力のbytes／str／None統一、空・壊れたPDFの拒否、現在入力と証拠の結合、空manifest拒否、reconcileによる外部結果照会 |
| T20 世代保存・再実行制御 | 実装済み・異常系確認済み | 検査前上書きと同一パス原本破損を防止。fsyncを含む永続化順序、ワークスペース全体の単一実行、成果物の再照合。実停電試験は未実施 |

## 修正の要点

1. 編集バッチ中はIDとElementの対応を固定し、位置からの再採番を行わない。削除した要素と子孫への後続操作はバッチ全体を未適用にする。備考の対象範囲外の変更は拒否する。
2. 属性変更は開始タグの局所置換で行い、対象外バイトの一致、再解析、意味差分で保全を確認する。文字コード、BOM、改行、宣言を保持する。
3. 入力は開始時にバイト列として固定し、検査を通った候補だけを不変世代へ確定してから任意パスへ書き出す。入力と出力が同じ実体なら中止する。
4. 実行入力は専用ディレクトリへ固定し、CSV内容のハッシュを入力同一性に含める。結果不明はUNKNOWNとして残し、照会結果を添えたreconcileでだけ状態を変える。
5. 受入記録は確定世代の複製へ書き、世代ディレクトリを不変に保つ。passには非空の証拠、ハッシュ一致、現在入力との一致を要求する。

## 検証結果と限界

2026-09-13のmacOS／Python 3.13.5で79件の試験が成功する。内訳は既存27件（うち2件はhost_os必須化とDesigner証拠の必須項目に合わせて呼出を更新）、R01〜R20を受入条件へ置き換えた回帰45件、CLIとserviceの統合・件数回帰7件である。

```bash
PYTHONPATH=src:tests python3 -m unittest discover -s tests -q
```

修正前の観察記録は[レビュー証拠](doc/review-evidence/20260913/README.md)に残す。`reproduce.py`は修正前のAPIと契約に対する記録であり、修正後はそのまま実行しない。期待値は回帰試験`tests/test_regression.py`が持つ。

確認できていない範囲は次のとおり。画像・PDF候補から新規XMLまでの一括生成、Windows実機、実停電、対象SVF版のXML開閉・改ページ・出力、フォント差、UCX/API結果照会、実LLM、実PDF・実画像の抽出精度、罫線抽出、大容量CSVの測定。これらを自動試験済みとして扱わない。

対象環境のベースXML、製品・版・SP、実行OS、実行方法、フォントと素材、Field／Record対応、確認用CSVと期待PDFを揃え、実機受入を実施する。
